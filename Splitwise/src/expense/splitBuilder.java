package expense;

import user.User;

import java.util.List;

public class splitBuilder {
//    public List<Split> buildSplit(double amount, List<User> participants, SplitType splitType) {
//        return switch (splitType) {
//            case EQUAL -> createSplits(splitType, amount, participants, null);
////            case EXACT -> createSplits(splitType, amount, participants, exactAmounts);
////            case PERCENT -> createSplits(splitType, amount, participants, exactAmounts);
//            default -> throw new IllegalArgumentException("Invalid split type");
//        };
//    }
}
